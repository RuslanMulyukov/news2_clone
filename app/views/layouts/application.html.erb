<!DOCTYPE html>
<html>
<head>
  <title>Web App</title>
  <%= stylesheet_link_tag "web-app-theme/base", "web-app-theme/themes/default/style", "web-app-theme/override", :cache => true %>
  <%= javascript_include_tag :defaults, "rails.validations", "rails.validations.custom", :cache => true %>
  <%= include_tiny_mce_if_needed %>
  <%= csrf_meta_tag %>
</head>
<body>
  <div id="container">
    <div id="header">
      <h1><a href="/">NEWS2.RU by Ruslan Mulyukov</a></h1>
      <div id="user-navigation">
        <ul class="wat-cf">
          <% if user_signed_in? %>
            <li><a href="<%= root_path %>" style="color: white;"><%= t("web-app-theme.profile", :default => "Main") %></a></li>
            <li><a href="<%= user_path(current_user) %>" style="color: white;"><%= t("web-app-theme.profile", :default => "Profile") %></a></li>
            <li><a href="<%= edit_user_path(current_user) %>" style="color: white;"><%= t("web-app-theme.settings", :default => "Setting") %></a></li>
            <li><a href="<%= destroy_user_session_path %>" class="logout"><%= t("web-app-theme.logout", :default => "Logout") %></a></li>
          <% end %>
        </ul>
      </div>
      <div id="main-navigation">
        <ul class="wat-cf"></ul>
      </div>
    </div>    
    <div id="wrapper" class="wat-cf">
      <div class="flash">
        <% flash.each do |type, message| -%>
          <div class="message <%= type %>">
            <p><%= message %></p>
          </div>
        <% end -%>
      </div>
      <div id="main">
        <%= yield %>
        <div id="footer">
          <div class="block">
            <p>Copyright &copy; <%= Time.now.year %> Web App.</p>
          </div>      
        </div>
      </div>
      <div id="sidebar">
        <%= yield :sidebar %>
      </div>
    </div>    
  </div>
</body>
</html>
