<% if @error.present? %>
  alert('Возникли проблемы при добавлении Вашего комментария, попробуйте снова!');
<% else %>
  $("div.comments_list").html("<%= escape_javascript(render(@comments)) %>");
  $(".comments_counter").html("<%= @comments.count %>");
<% end %>